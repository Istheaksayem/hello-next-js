import Title from '@/component/Title';
import React from 'react';

const Settings = () => {
    return (
        <div>
            <Title>settings</Title>
        </div>
    );
};

export default Settings;